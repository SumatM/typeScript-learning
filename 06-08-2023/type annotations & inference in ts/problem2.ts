// Variable Annotation:
// Declare a variable person with the type annotation of an object containing name (string) and age (number) properties.


type Person = {
    name:string,
    age: number
}

const chunnu:Person = {name:'chunnu',age:11}