// Write a function called calculateArea that calculates the area of a rectangle. It should take the length (number) and width (number) as parameters and return the calculated area (number).

function calculateArea(l: number, w: number): number {
  let area: number = l * w;
  return area;
}

console.log(calculateArea(5, 6));
