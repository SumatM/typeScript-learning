// Create a tuple representing a book's information: title (string), author (string), and publication year (number). Declare a variable of this tuple type and initialize it with sample data.


type BookInfo = [string,string,number]

const book : BookInfo = ['Alchemist','Paul', 2001]