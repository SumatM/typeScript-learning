// Create a function that takes two parameters (both numbers) and returns their sum. Use type annotations for function parameters and return type.


function sum(a:number,b:number):number{
    return a+b;
}

console.log(sum(1,2))